{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1>Welcome</h1>
    <h2>Listing livres</h2>
    <div class="tab container">
        <table class="table table-striped table-dark">
            <thead>
            <tr>
                <th scope="col">Titre</th>
                <th scope="col">Liens descriptif </th>
                {# <th>Votes</th>
                {# <th>Genre</th>#}
                <th>Modifications</th>
                <th>Suppression</th>
            </tr>
            </thead>
            <tbody>

            {% for key in list_livre%}

                <tr>
                    <th>{{ key.titre }}</th>
                    {# recuperation de la valeur du click pour traitement pour la requete sql -> recup de l'annee selectionné dans val #}
                    <th> <a class=" list-group-item list-group-item-action" href="#">{{key.titre }}</a></th>
                    {#<th>{{ key.votes }}</th>
                    <th>{{ key.genre_id}}</th>#}
                    <th><a class=" list-group-item list-group-item-action" href="#">Edit</a></th>
                    <th><a class=" list-group-item list-group-item-action" href="#">Delete</a></th>

                </tr>
            {% endfor %}
            </tbody>
        </table>


    </div>


</div>
{% endblock %}
